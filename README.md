# No-Code Site Builder  
(React + Node.js + PostgreSQL + Docker)

Веб-приложение “no-code” конструктора сайтов: визуальный редактор (Canvas), библиотека блоков, панель свойств, проекты/страницы, авторизация, экспорт сайта в ZIP (HTML + CSS).

---

## Содержание
- Возможности
- Технологии
- Архитектура и ключевые концепции
- Структура проекта
- Быстрый старт (Docker)
- Проверка API
- Локальный запуск без Docker (опционально)
- Переменные окружения
- Типовые проблемы и решения
- Roadmap
- Лицензия

---

## Возможности

### 🎨 Визуальный редактор
- Drag & Drop Canvas (Figma-style)
- Перемещение элементов мышью
- Изменение размеров элементов
- Автоматическое увеличение `z-index` при перетаскивании
- Свободное перекрытие элементов

### 🧩 Библиотека блоков
Поддерживаемые блоки:
- Heading (Заголовок)
- Text (Текст)
- Button (Кнопка со ссылкой)
- Image (Изображение)
- ZeroBlock (секция / визуальный контейнер)
- Tag (бейдж / метка)
- Divider (разделитель)

### 🎛 Панель свойств
- Позиция и размеры (X / Y / Z / Width / Height)
- Дизайн: фон, цвет текста, скругление, отступы, тени
- Типографика: размер и толщина шрифта, выравнивание
- Технические атрибуты (текст, ссылки, служебные поля)

### 🎨 Пресеты дизайна
Готовые стили, применяемые в один клик:
- Card
- Soft
- Dark
- Glass
- Gradient
- Minimal

Пресеты работают для **любого блока**, включая ZeroBlock.

### 📄 Проекты и страницы
- Создание и удаление проектов
- Многостраничные проекты
- Навигация между страницами
- Экспорт всех страниц проекта

### 🔐 Авторизация
- Регистрация / вход
- JWT-аутентификация
- Привязка проектов к пользователю
- Защищённый доступ к редактору

### 📦 Экспорт
- Экспорт проекта в ZIP-архив
- Чистый HTML + CSS
- Корректная навигация между страницами
- Автоматический расчёт высоты Canvas
- Рабочие ссылки (`<a href>`)
- Готово к публикации на любом хостинге

---

## Технологии

### Frontend
- React 18+
- Vite 5+
- Redux Toolkit + RTK Query
- Собственный Canvas (без сторонних DnD библиотек)
- CSS Tokens + Globals

### Backend
- Node.js 20+ (ESM)
- Express.js
- Sequelize ORM
- PostgreSQL 15
- JWT (Auth)
- Archiver (экспорт ZIP)

### Инфраструктура
- Docker
- Docker Compose
- Nginx (reverse proxy для frontend и backend)

---

## Архитектура и ключевые концепции

- **Canvas** — центральный визуальный холст редактора
- **Element** — атомарная сущность (позиция, размер, стиль, атрибуты)
- **Block Registry** — единый реестр типов блоков
- **ZeroBlock** — визуальная секция (фон/контейнер, без вложенности)
- **Properties Panel** — единая точка редактирования всех свойств
- **Export Service** — генерация статического сайта

---

## Структура проекта (кратко)

```
frontend/           # React + Vite
backend/            # Express + Sequelize
nginx/              # Nginx конфигурация
docker-compose.yml  # Запуск всей системы
```

---

## Быстрый старт (Docker) — рекомендуемый способ

### Требования
- Docker
- Docker Compose

Проверка:
```
docker --version
docker compose version
```

### Запуск
```
docker compose up -d --build
```

### Открыть приложение
- UI: http://localhost
- API: http://localhost:8080/api/health

---

## Проверка API

Base URL:
```
http://localhost:8080/api
```

Примеры:
- POST /auth/register
- POST /auth/login
- GET /auth/me
- GET /projects
- POST /projects
- GET /export/project/:projectId.zip

---

## Локальный запуск без Docker (опционально)

Требуется:
- Node.js 20+
- PostgreSQL 15+

Frontend:
```
cd frontend
npm install
npm run dev
```

Backend:
```
cd backend
npm install
npm run dev
```

---

## Переменные окружения

### Backend (.env)
```
DB_HOST=postgres
DB_PORT=5432
DB_NAME=nsb
DB_USER=nsb
DB_PASSWORD=nsb
JWT_SECRET=supersecret
```

### Frontend
```
VITE_API_BASE=/api
```

---

## Типовые проблемы и решения

### 401 Unauthorized
- пользователь не авторизован
- токен отсутствует или истёк

### 502 Bad Gateway
- backend не запущен
- проверить: docker compose logs backend

### Экспорт не работает
- убедиться, что проект принадлежит текущему пользователю
- проверить endpoint `/api/export/project/:id.zip`

---


